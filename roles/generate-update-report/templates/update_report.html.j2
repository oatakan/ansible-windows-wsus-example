<!DOCTYPE html>
<html>
   <head>
      <title>Report Table for Windows Updates per Host</title>
   </head>

   <body>
      <table border = "1" cellpadding = "5" cellspacing = "5">
         <tr>
            <th>Windows Host</th>
            <th>OS</th>
            <th>List of Required Updates/Packages</th>
         </tr>
{% for host in ansible_play_hosts_all %}
{% if hostvars[host].ansible_distribution is defined %}
         <tr>
            <td>{{ host }}</td>
            <td>{{ hostvars[host].ansible_distribution }}</td>
            <td>
               <ul>
{% if hostvars[host].available_updates.updates is defined and hostvars[host].available_updates.found_update_count > 0  %}
{% for key, value in hostvars[host].available_updates.updates.items() %}
                  <li>{{ value.title }}</li>
{% endfor %}
{% else %}
                  <li>Compliant</li>
{% endif %}
               </ul>
            </td>
         </tr>
{% endif %}
{% endfor %}
      </table>
   </body>
</html>